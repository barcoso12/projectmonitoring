{
  "name": "project-monitoring-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "install-backend": "npm install --workspace=backend",
    "install-frontend": "npm install --workspace=frontend",
    "build-frontend": "npm run build --workspace=frontend",
    "build-backend": "npm run build --workspace=backend || true",
    "start-backend": "npm run start --workspace=backend",
    "build": "echo 'Root build finished. Use workspace-specific build commands if necessary.'"
  },
  "workspaces": [
    "backend",
    "frontend"
  ]
}
